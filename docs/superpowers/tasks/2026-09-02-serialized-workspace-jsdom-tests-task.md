# Serialized Workspace jsdom Tests Tasks

1. Pin the root test command to pnpm workspace concurrency one and verify the
   two package test suites still execute once.
2. Run repeated root/Poetry verification without ambient concurrency settings.
3. Remove the resolved jsdom BACKLOG row and record one handoff with command
   evidence.
