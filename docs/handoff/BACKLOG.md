# Deferred Work Backlog

Single living index of every deferred (accepted-but-not-done) item across all
handoffs. Each item is ONE line pointing to the handoff that holds the full
context and ruling. Remove the line when the item is done — done work lives in
git history, not here.

Scope guard: this file indexes DEFERRED work only. Gates and requirements
belong to `docs/20-IMPLEMENTATION_PLAN.md`; current status of a gate belongs
to the living domain doc (e.g. `docs/operations/`). Do not duplicate those
here — link them at most.

`Implement by` is the latest permitted delivery gate: “Before Child N” blocks
that child from starting; operational and conditional entries name the exact
trigger instead. Non-load-bearing hygiene rows use `Before Phase 2 closure
(after Child 9)` and do not block the next child by default.

## Active deferred work

These items have a current implementation path or an outstanding acceptance
obligation. Their `Implement by` values are delivery gates, not reasons to
wait before starting work.

| Added | Domain | Item | Implement by | Details |
|---|---|---|---|---|
| 2026-08-18 | small-file-sync | Child-4 reference-device evidence (Desktop + Mobile Obsidian) PENDING — operator records sanitized rows per the documented procedure; automated gates cannot substitute | Before Child 9 acceptance closure | [handoff §5](2026-08-18-plugin-journal-small-file-sync.md) |
| 2026-08-24 | closed-reason-surfacing | Live smoke round of the remediation surfaces: wrong-origin token ✓, terminal cleared-reason ✓, staleness ✓ (all observed 2026-09-01) — REMAINING is the lifecycle rejection ring readback (locator conflict on restore); re-fires on 2026-09-03 were blocked by device-sync edges — TWO of the three findings now fixed (`186286d` restart invalidation, `7e82701` retry bound; the burst-loss row remains) — before any journey step; the stack/readback path/two-vault fixture are proven | Before Child 9 operations acceptance | [handoff §4](2026-08-24-closed-reason-surfacing-remediation.md); [round handoff](2026-09-01-policy-diagnostics-metrics-sink-and-live-smoke.md); [block handoff](2026-09-03-lifecycle-rejection-ring-live-readback.md) |
| 2026-09-03 | device-sync | Untitled-transit burst (create Untitled → move across folders → rename) no longer hard-stops the journal (fixed `29f65f5`) but still LOSES the rename chain: the canonical source stays at the old path and is re-downloaded into every vault while the renamed path parks `blocked_conflict` untracked | Before Child 9 operations acceptance | [robustness handoff](2026-09-03-manifest-restart-and-apply-robustness.md); [block handoff addendum](2026-09-03-lifecycle-rejection-ring-live-readback.md); [recovery plan](../superpowers/plans/2026-09-03-untitled-transit-rename-chain-recovery-plan.md) |
| 2026-08-28 | multipart-upload | Physical Mobile matrix PENDING (mobile live test, needs a physical device) — record the four sanitized rows per the runbook; on the same live round, note whether the journey's re-fire recovery path (lost policy-fixture capture) triggered; also on that same round, re-verify the rebuild reconcile-first fix (a fresh journal over a non-empty vault reconciles first — [child-8 handoff](2026-08-29-device-sync-child8-unblock-smoke-prep.md)) | Before Child 9 operations acceptance | [handoff §deferred](2026-08-28-resumable-multipart-mobile-upload.md) |
| 2026-09-02 | source-conflicts | Operator Desktop Conflict Inbox journey PENDING (manual gate deferred by operator decision 2026-09-02): re-stand the CI project (`CI=true bash .local/serve-live-ci.sh up knowledge-ci-source-conflicts-20260902`, plus `tools/obsidian_live_acceptance_bootstrap.py` for the TOTP/policy HTTP half if needed), run the 8-step runbook plus the final review's 6 additional exercises (keep-remote/keep-local live, stale-successor loop, local-apply recovery, capture lane, binary render, policy recheck) on a dedicated Vault test, record sanitized evidence (outcome, reason token, count, timestamp), then `serve-live-ci.sh down` | Before Child 9 operations acceptance | [handoff §deferred](2026-09-02-source-conflict-capture-and-resolution.md); runbook `docs/operations/source-conflict-resolution.md` |

## Feature gaps

These are intentionally unsupported product capabilities, not defects in the current contract. They need a new approved product/design plan before work begins, and must land by their stated customer or operational commitment.

| Added | Domain | Item | Implement by | Details |
|---|---|---|---|---|
| 2026-09-01 | small-file-sync | Large-vault cold sync is unsupported by design today: the outbound drain is serial per event (live-measured ~6–13s/event end-to-end through the durable Temporal commit) and capture fail-closes at 10,000 pending events — a bulk-import path (batch commits, parallel admission, or manifest-driven bulk mode) is the architectural lever; manifest side already pages 500/page — DECIDED (2026-09-03, product-owner delegation): batch-commit admission (N events per PostgreSQL transaction with parallel content verification; Dropbox Nucleus batching precedent) | Before any large-vault onboarding is promised | [round handoff](2026-09-01-policy-diagnostics-metrics-sink-and-live-smoke.md) |
| 2026-09-03 | device-sync | Inbound propagation is poll-based only (30s foreground tick, ≤200 events/page): no server-push channel (WebSocket/SSE/long-poll) alongside the pull/ack cursor spine — sub-30s Google-Drive-class propagation needs a push lane that must not bypass cursor discipline or the contiguous-sequence apply lattice — DECIDED (2026-09-03, product-owner delegation): Dropbox-style long-poll on the existing pull route first (hanging GET above the device cursor returns as soon as the next sequence commits; plain HTTPS + existing bearer auth, cursor spine untouched; wake the hanging pull via PostgreSQL LISTEN/NOTIFY on event commit — no new worker or queue; WebSocket/SSE only if long-poll proves insufficient) | Before production activation | [sync contract](../04-OBSIDIAN_SYNC_AND_SOURCES.md); [cursor runbook](../operations/device-cursor-manifest-reconciliation.md) |
| 2026-09-03 | multipart-upload | Hard 100 MiB transfer ceiling (multipart ≤13 parts × 8 MiB; larger files are born-terminal `blocked_size`) and every transfer is full-content — no content-defined chunking, dedup, or delta upload on either lane; large-file support and big-file edit latency need a chunking redesign — DECIDED (2026-09-03, product-owner delegation): content-addressed fixed blocks (Dropbox 4 MiB block precedent) with hash-admission at preflight (server instantly admits blocks it already stores — Drive md5Checksum dedup precedent), updating only changed blocks; evolve the existing resumable multipart lane rather than a parallel path | Before the first >100 MiB content type is admitted to the vault contract | [multipart runbook](../operations/resumable-multipart-upload.md); [journal runbook](../operations/plugin-journal-small-file-sync.md) |
| 2026-09-03 | device-sync | Every device converges on the full vault: no per-device folder scoping and no placeholder/on-demand streaming mode — storage-bounded devices (mobile) pay full vault storage plus full manifest enumeration (500 entries/page, 100k-entry run cap) | Before any storage-bounded device profile is promised | [sync contract](../04-OBSIDIAN_SYNC_AND_SOURCES.md); [cursor runbook](../operations/device-cursor-manifest-reconciliation.md) |
| 2026-09-03 | api-contract | Vault content is reachable only through the Obsidian plugin (desktop + mobile): no web reader, OS-level folder exposure, or third-party app API surface — ubiquitous Drive-class access would ride the same canonical event/version surfaces | Before any non-Obsidian client surface is promised | [API integration doc](../12-API_MCP_AND_AGENT_INTEGRATION.md); [web app doc](../13-WEB_APP_AND_ADMIN_DASHBOARD.md) |
| 2026-09-03 | source-conflicts | The version lineage (base versions, conflict parents, tombstones) is fully stored server-side but has no user-facing browse or point-in-time restore surface — a "restore this version" UX over `source_versions` and conflict records (distinct from the pending Conflict Inbox operator acceptance row above) | Before production activation | [conflict resolution runbook](../operations/source-conflict-resolution.md) |
| 2026-09-03 | device-sync | No user-facing sync traffic controls (bandwidth throttle, pause/resume, schedule): only the internal jittered backoff 1s–5min and the repair barrier; metered/constrained-network management is absent | When a constrained-network usage profile is reported | [sync error tracing](../operations/sync-error-tracing.md) |
| 2026-09-03 | small-file-sync | The sql.js journal persistence model does not scale to large vaults: the whole DB lives in the WASM heap and every mutation exports the full image, double-hashes it, and writes+reads-back the entire file (O(DB size) per write, quadratic cumulative; 5–6 full commits per inbound remote event); terminal `journal_events`/`remote_apply_operations` rows and never-matched `echo_markers` are kept forever (no vacuum, no index on `remote_apply_operations.state`); the convergence snapshot truncates enumeration at 10,000 files — DECIDED (2026-09-03, product-owner delegation): replace sql.js whole-image generations with an append-log segment store (sealed immutable segments + bounded active segment + compacted snapshots, checksummed records with a torn-tail rule, state RAM-resident behind the unchanged `DeviceSyncRepository` port; writes become O(record) instead of O(DB size), retention becomes compaction; mobile-safe via DataAdapter-only writes, no VFS dependency) | Before any large-vault onboarding is promised | [journal runbook](../operations/plugin-journal-small-file-sync.md); [design spec](../superpowers/specs/2026-08-18-plugin-journal-and-small-file-sync-design.md) |
| 2026-09-03 | small-file-sync | Snapshot convergence scan full-reads and full-hashes every enumerated file with no stat pre-filter (verified absent in `capture.ts` 2026-09-03): a 12k-file vault re-hashes all 12k files per scan — add an mtime+size pre-filter against the last scan's stats so only changed files are read and hashed, and use the same stat pass to detect vanished files (delete/rename) instead of relying on lossy watchers; standard Google Drive/Dropbox client technique | Before any large-vault onboarding is promised | [journal runbook](../operations/plugin-journal-small-file-sync.md) |
| 2026-09-03 | device-sync | Manifest planner queries lack serving indexes at vault scale: the per-page locator-candidates and tombstone-candidates lookups (`device_manifest_store.py`) fall back to workspace-wide scans because only partial unique indexes exist on `source_locators`/`source_tombstones`, and the finalize open-tombstone lateral (including the O(total-workspace-sources) canonical-only downloads statement) owns no index — the query-plan acceptance deliberately keeps `source_tombstones` at 300 rows and pins none of these statements | Before any large-vault onboarding is promised | [data model doc](../07-POSTGRESQL_DATA_MODEL.md); [plan-pin test](../../tests/integration/device_sync/test_device_sync_query_plans.py) |
| 2026-09-03 | device-sync | The 100,000-entry manifest cap is aspirational: finalize runs in one transaction holding the run `FOR UPDATE` while every statement must fit `statement_timeout = 15s`, and the canonical-only downloads scan is O(total workspace sources) in a single statement (3 lateral probes per source) — the realizable finalize size sits far below the schema cap and needs chunked/fenced planning | Before any large-vault onboarding is promised | [data model doc](../07-POSTGRESQL_DATA_MODEL.md) |
| 2026-09-03 | device-sync | Server-side retention is unbounded: no DELETE path exists anywhere — `sync_events` (the `minimum_acknowledged_sequence` compaction floor is exposed by `device_event_store.py` but no retention/compaction owner executes it), `source_locators`, `source_tombstones`, `audit_events`, and expired/completed `manifest_runs` rows (expiry only flips state; no cleanup job) all accumulate forever | Before production activation | [cursor runbook](../operations/device-cursor-manifest-reconciliation.md); [data model doc](../07-POSTGRESQL_DATA_MODEL.md) |

## Trigger-based deferred work

These items have no current implementation obligation. Reopen them only when
the stated trigger occurs; then handle them in the same change or dedicated
follow-up required by that trigger.

| Added | Domain | Item | Implement by | Details |
|---|---|---|---|---|
| 2026-08-15 | api-contract | `openapi-typescript@7.13.0` peer-declares `typescript@^5.x` while the workspace pins `6.0.3` (standing install warning; resurface on any pin bump) | At next TypeScript pin bump | [handoff §1](2026-08-15-api-runtime-contract-foundation.md) |
| 2026-08-20 | sources | Structural enforcement of the "sensitive value object must redact `__repr__`" contract is deferred — until a fourth class needs it, prefer repetition over a `RedactedString` mixin/Protocol. Reopen this row when a new sensitive-string value object is added without a redacted repr | When a fourth sensitive value object is added | [handoff §4](2026-08-14-source-version-publication.md) |
| 2026-08-24 | exclusion-policy | Unknown future `exclusion_policy_*` code silently no-ops out of the small-file rejection ring (`_record_policy_rejection` docstring contract, not runtime-enforced) — a new code must choose a SYSTEM/DENIAL side and extend the ring map in the same diff | Before the next exclusion-policy error code is added | [handoff §5.4](2026-08-24-policy-observability-remediation.md) |
| 2026-09-02 | backup/source-conflicts | The new `knowledge.source_conflicts` canonical table is absent from the backup snapshot's fixed `SNAPSHOT_LOCK_ORDER` (`packages/postgresql-source-store/src/postgresql_source_store/backup_snapshot.py`, manifest v4 stays at 35 tables), so conflict rows fall outside the canonical snapshot's quiescing lock order — extend the order when the backup manifest is next versioned | Before the next backup-manifest version bump (v5) | [handoff §deferred](2026-09-02-source-conflict-capture-and-resolution.md) |
| 2026-09-03 | vault-mutation | Follow-up hardening plan for the shared atomic vault mutation primitive: cause-bearing `AtomicVaultMutationFailure` (read-throw vs fingerprint mismatch, target-absent distinction or `requireOccupiedTarget` flag — removes the writer wrapper's positional null-read heuristic and the prove_base read-throw→divergence-token nuance), a formal pre-first-visible-mutation hook retiring the sequencing wrapper's implicit protocol, the `conflict_sibling_cleanup_failed` closed token for ignored cleanup refusals, and dedup of the writer's residual `hashesTo`/`#restoreRollback` | Before the next atomic-vault-mutation contract extension | [handoff §next-actions](2026-09-03-conflict-vault-apply-hardening.md) |
| 2026-09-03 | source-conflicts | A conflict apply replace-stage failure leaves one exact-token rollback sibling un-reclaimed by retries (data-preserving, spec req 3 compliant — the retry takes the created shape over an absent target); reclaiming requires a recovery sweep with the naming-contract proof | When a conflict vault-apply recovery sweep is designed | [handoff §deferred](2026-09-03-conflict-vault-apply-hardening.md) |
| 2026-09-03 | multipart-upload | Block-content compression/encoding (Dropbox Broccoli-style "sync faster by syncing less") is premature while transfers are still full-content or whole-block — reopen only after the DECIDED content-addressed block lane (Feature gaps above) lands | When the content-addressed block lane lands | [Broccoli precedent](https://dropbox.tech/infrastructure/-broccoli--syncing-faster-by-syncing-less) |
